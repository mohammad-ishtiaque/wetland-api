.env
uploads/
node_modules/

# Dev-only diagnostic/test scripts — not part of the shipped API, should
# never end up in a production deploy or the client-delivered repo.
diagnose.js
test-api.js
test-null.js
test-subscription.mjs

# Diagnostic/audit tooling — hits live 3rd-party APIs to check station
# health nationwide, not something the running server needs. Flagging this
# one specifically: unlike the files above, this is a reusable ops tool
# (see its own header comment), not a throwaway test — if you'd rather keep
# it versioned for the team, just remove this line.
scripts/
